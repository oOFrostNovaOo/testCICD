network:
  version: 2
  ethernets:
    ens33:
      dhcp4: no
      addresses: [{{ ip_address }}/{{ subnet_mask }}]
      gateway4: {{ gateway }}
      nameservers:
        addresses: {{ dns_servers }}
